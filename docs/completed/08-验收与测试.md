# 验收与测试（已完成）

## 脚本一览

| 脚本 | 用途 | 通过标准 |
|------|------|----------|
| `scripts/smoke_relay.py` | S0–S6 分阶段 smoke | 15/15 |
| `scripts/eval_l2.py` | L2 25 样本 | routing ≥75%，impl/debug 零失误 |
| `scripts/eval_decomposer.py` | 10 条拆分样本 | ≥80%（当前 10/10） |
| `scripts/eval_e2e.py` | 4 设计场景 | 全部断言 |
| `scripts/test_budget_adapter.py` | 预算 ratio 0–1 | 不崩溃 |
| `scripts/test_cursor_cli.py` | CLI stats | 退出 0 |
| `scripts/run_acceptance.py` | **一键跑以上全部** | 6/6 |

## 提交前约定

每个开发任务完成后：

```bash
python scripts/run_acceptance.py
git commit && git push
```

## 样本配置

- `config/l2_eval_samples.json` — 25 条 L2 标注
- `config/decomposer_eval_samples.json` — 10 条拆分（6 应拆 + 4 不拆）

## Windows 注意

`smoke_relay.py` S5 子进程已设 `encoding=utf-8`，避免 GBK 解码 L1 输出失败。
