# 中转站接入（已完成）

## Provider 配置

密钥位置：`~/.llm-router/.env`（**不入库**）  
模板：`config/relay.env.example`

| Provider | Base URL | 说明 |
|----------|----------|------|
| OpenAI（Ccode） | `https://all-api.ccode.dev/v1` | 走中转站 |
| Anthropic（Ccode） | `https://all-api.ccode.dev` | 走中转站 |
| DeepSeek | `https://api.deepseek.com/v1` | Ccode 401，走官方 API |

## 模型映射

`config/relay_models.yaml`：

| 逻辑名 | 实际上游 ID |
|--------|-------------|
| `openai/gpt-4o` | `openai/gpt-5.5` |
| `openai/gpt-4o-mini` | `openai/gpt-5.4-mini` |
| `anthropic/claude-sonnet-4-6` | 同名 |
| `anthropic/claude-opus-4-6` | 同名 |
| `deepseek/deepseek-chat` | `deepseek/deepseek-v4-flash` |

## 实现文件

- `src/relay_config.py` — 加载 `.env`，注入 `OPENAI_API_BASE` 等 LiteLLM 环境变量
- `src/relay_llm.py` — 统一 `call_llm()`，供 orchestrator / classifier / decomposer 使用

## 绕过 llm-router 打包缺陷

已安装 `llm-router` 缺少 `policies/standard.yaml`，`llm_router.providers` 无法导入。  
**当前方案：** 不依赖 llm-router 的 `call_llm`，直接用 `relay_llm.py` + LiteLLM。

## Smoke 验收

`scripts/smoke_relay.py` — S0 环境、S1 models 列表、S2–S4 三 Provider 实调、S5 L1、S6 端到端 → **15/15**
