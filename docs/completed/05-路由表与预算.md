# 路由表与预算（已完成）

## 核心映射

文件：`src/routing_table.py`

```python
TASK_TO_MODEL = {
    "architecture":      "anthropic/claude-sonnet-4-6",
    "system_design":     "anthropic/claude-sonnet-4-6",
    "deep_reasoning":    "anthropic/claude-opus-4-6",
    "implementation":    "openai/gpt-4o",
    "debugging":         "openai/gpt-4o",
    "refactor":          "openai/gpt-4o",
    "boilerplate":       "deepseek/deepseek-chat",
    "bulk_generation":   "deepseek/deepseek-chat",
    "data_processing":   "deepseek/deepseek-chat",
    "code_patch":        "cursor_queue",
    "file_edit":         "cursor_queue",
    "uncertain":         "openai/gpt-4o",
}
```

经 `relay_config.resolve_model()` 映射到中转站真实模型 ID。

## 任务感知降级链

每条 `task_type` 独立 `FALLBACK_CHAINS`：**架构类不下探到 DeepSeek**。

## 渐进预算四区

| 消耗比例 | 区域 | 行为 |
|----------|------|------|
| 0–60% | green | 不干预 |
| 60–75% | yellow | 非关键任务降一档 |
| 75–90% | orange | 非关键任务降到最便宜可用 |
| 90%+ | red | 仅 architecture/deep_reasoning 保持原模型 |

关键任务：`architecture`、`system_design`、`deep_reasoning`。

## 预算数据源

- 文件：`src/budget_adapter.py`
- 读取 `llm_router.budget.get_budget_state(provider)`，取 anthropic/openai/deepseek 最大 `pressure`
- orchestrator `_get_budget_ratio()` 已接入
- 看板 `/api/status` 展示 `budget_ratio`（带 3s 超时防阻塞）

## 策略 YAML

`src/policies/custom.yaml` — llm-router 策略文件（备用，当前主路径走自研 routing_table）。
