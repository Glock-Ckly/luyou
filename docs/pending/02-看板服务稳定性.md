# 看板服务稳定性（待完成）

## 现状

`scripts/dashboard_server.py` 使用标准库 `HTTPServer`（单线程）：

- 多实例同时监听 1785 → API 超时
- 客户端刷新断开 → 曾导致 `ConnectionAbortedError` 退出（已捕获，但非根本解决）
- `budget_ratio` 查询曾阻塞请求（已加 3s 线程超时）
- 无进程守护：崩溃后需手动 `python scripts/dashboard_server.py`

## 目标

- [ ] 启动前检测端口占用，自动 kill 或换端口
- [ ] 单实例锁文件（如 `~/.llm-router/dashboard.pid`）
- [ ] 可选：改用 `uvicorn` + FastAPI 多 worker 或 systemd/计划任务自启
- [ ] 健康检查 `GET /api/health` 轻量响应（不拉 budget）

## 验收

连续刷新 50 次 `/` 与 `/api/status`，进程不退出，响应 < 2s。
