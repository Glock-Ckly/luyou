# 子任务并行执行（待完成）

## 现状

`orchestrator.handle()` 对 `decomposer_result.subtasks` **顺序 for 循环**：

- 分类 → 路由 → 执行 → 校验，一个接一个
- 设计文档示例 2 写「5 个子任务并行执行」— **未实现**

## 目标

```python
results = await asyncio.gather(*[
    self._process_subtask(st) for st in decomposer_result.subtasks
])
```

## 注意点

- 并发 API 调用 → 速率限制与成本尖峰
- Cursor Queue 推送顺序无关
- 聚合 `_aggregate` 需保持 subtask index 排序

## 验收

- [ ] 3 子任务拆分任务总耗时 < 顺序版 60%（同网络条件下）
- [ ] `eval_e2e` 场景 2 仍 PASS
