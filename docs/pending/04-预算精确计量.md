# 预算精确计量（待完成）

## 现状

- `budget_adapter.get_budget_ratio()` 已接入 orchestrator
- 读 `llm_router.budget.get_budget_state(provider)` → `pressure` 0–1
- 无 Claude subscription 时，`usage.json` 可能缺失 → **stale pressure floor 0.5**（乐观/保守折中）
- 未按任务类型分 provider 预算；未暴露 spend_usd/cap 到看板

## 设计目标（来自 multi-llm-router-design.md）

四区渐进：0–60% green / 60–75% yellow / 75–90% orange / 90%+ red  
**路由表侧已实现**；缺的是**真实 spend 数据源**。

## 待办

- [ ] 配置 `llm-router budget set <provider> <USD>` 月度 cap
- [ ] 确保 session-start hook 写 `~/.llm-router/usage.json`（headless install 已装）
- [ ] orchestrator 记录每次 `call_llm` cost 写入本地 SQLite/JSON
- [ ] 看板展示各 provider spend / cap / zone 名称

## 验收

人工将 cap 设为极低 → implementation 任务在 orange 区应路由到 deepseek（eval_e2e S4 逻辑 + 真实 ratio>0.75）。
