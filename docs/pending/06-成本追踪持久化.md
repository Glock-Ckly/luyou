# 成本追踪持久化（待完成）

## 现状

- `relay_llm.LLMResponse` 含 `cost_usd`，orchestrator 累加 `total_cost_usd`
- **不落库**；会话结束即丢失
- llm-router 自带 `~/.llm-router/usage.db`（hooks 路径），与 orchestrator 未打通

## 目标

| 能力 | 说明 |
|------|------|
| 每次路由一条记录 | prompt 摘要、task_type、model、tokens、cost、时间 |
| 查询 CLI | `python scripts/cost_report.py --day` |
| 看板展示 | 今日 spend、按模型占比 |

## 可选实现

- 复用 llm-router SQLite schema
- 或轻量 `~/.llm-router/luyou_cost.jsonl`

## 验收

跑 5 次 `handle_prompt` 后，报表总额与 LiteLLM 返回 cost 之和误差 < 1%。
