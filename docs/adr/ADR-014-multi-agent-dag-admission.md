# ADR-014: Multi-Agent DAG Parallel Admission

## 背景

盲目并行会造成写路径冲突、读取未完成 Artifact、预算超支和不可复现合并，多 Agent 数量本身不等于质量。

## 决策

仅当写路径不相交、依赖 Artifact 已完成、预算与 Provider 配额允许且每个节点有独立幂等键时并行。写冲突是硬阻塞并强制串行。

## 后果

并行结果更可复现且可归责，综合必须保留来源和验证证据。代价是并行度较保守，需要 DAG 冲突检测、租约和确定性调度。
