# Skill Contract

A repository Skill must define a narrow trigger description, deterministic workflow, explicit safety boundaries, reusable scripts or references only when needed, and an executable validation step.

Skills must not become hidden autonomous policy. They may guide delivery, validate adapter boundaries or audit reliability. They must remain independently testable and must not receive credentials.
