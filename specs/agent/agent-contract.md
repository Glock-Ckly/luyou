# Agent Contract

Repository agents are bounded role prompts for architecture, TDD implementation, Provider adapters and release audit.

Every agent definition must include mission, allowed write set, forbidden actions and exit criteria. Release audit remains independent and cannot change production code. Runtime autonomous routing agents, step budgets and cost budgets are deferred and must not be claimed as implemented.
