# Bounded Agents

These role prompts are intentionally non-autonomous. Assign one bounded task and an explicit write set. The Release and Audit Agent must remain independent from the implementation write set.
